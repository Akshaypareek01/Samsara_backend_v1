/** Max multipart file size Multer will buffer in memory. */
export const MAX_UPLOAD_FILE_SIZE_BYTES = 25 * 1024 * 1024;

/** User-facing label for {@link MAX_UPLOAD_FILE_SIZE_BYTES}. */
export const MAX_UPLOAD_FILE_SIZE_LABEL = '25MB';
